<div class="container-fluid col mt-2">
  <%= link_to "Back to Categories", categories_path, class:"btn btn-secondary" %>
  <div id="<%= dom_id category %>">
    <div class="d-inline-flex gap-3">
    <label class="fs-1 mt-2">Category: <%= category.category_name %></label>
    <%= link_to raw('<i class="fa-solid fa-circle-plus"></i> New Task'), new_category_task_path(@category), class: "btn btn-secondary m-auto" %>
    </div>
    </div>

