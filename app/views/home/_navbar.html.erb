<nav class="navbar navbar-expand-lg bg-dark text-light" data-bs-theme="dark">
  <div class="container-fluid">
    <div class="d-inline-flex">
    <i class="fa-solid fa-book-journal-whills fs-1"></i>
    <%= link_to "Journal Planner", root_path, class: "navbar-brand fs-4" %>
    </div>
    <div class="d-flex">
        <ul class="navbar-nav me-auto mb-2 mb-lg-0 gap-2">
            <% if user_signed_in? %>
            <div class="dropdown-center">
              <button class="btn btn-secondary dropdown-toggle" type="button" data-bs-toggle="dropdown" aria-expanded="false">
                    <i class="fa-solid fa-gear"></i>
              </button>
                <ul class="dropdown-menu dropdown-menu-end">
                  <li><%= link_to "Change Password", edit_user_registration_path, class: "btn btn-dark btn-sm dropdown-item mb-2" %></li>
                  <li><%= button_to "Logout", destroy_user_session_path, method: :delete, class: "btn btn-dark btn-sm dropdown-item mb-2" %></li>   
                </ul>
            </div>
            <% else %>
                
            <% end %>
        </ul>
    </div>
  </div>
</nav>
